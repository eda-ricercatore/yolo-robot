function outputString = imcompare(letter, height, width)

% http://stackoverflow.com/questions/16590176/how-to-iterate-over-datasets-rows-aka-observations
% load('letterdatabase');

% iterate over a dataset, resize the dataset's image to compare = imresize(letter, height, width);
% any value that has less than 50 pixels left is returned as the name.